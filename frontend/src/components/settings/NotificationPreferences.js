import SettingsSection from "@/components/settings/SettingsSection";
import PreferenceCheckbox from "@/components/settings/PreferenceCheckbox";
const options = [["licenceAlerts", "Licence expiry alerts", "Receive reminders before driver licence expiry."], ["maintenanceReminders", "Maintenance reminders", "Surface planned maintenance and follow-up activity."], ["tripNotifications", "Trip status notifications", "Show updates when dispatch status changes."], ["fuelExpenseAlerts", "Fuel and expense alerts", "Highlight recorded fuel and cost activity."], ["weeklySummary", "Weekly operational summary", "Prepare a weekly overview of fleet activity."]];
export default function NotificationPreferences({ settings, onChange }) { return <SettingsSection title="Notification preferences" description="Choose which operational reminders appear in your workspace.">{options.map(([key, label, description]) => <PreferenceCheckbox key={key} id={key} label={label} description={description} checked={settings[key]} onChange={(value) => onChange(key, value)} />)}</SettingsSection>; }
