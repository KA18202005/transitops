export default function PreferenceCheckbox({ id, label, description, checked, onChange }) { return <label htmlFor={id} className="flex cursor-pointer items-start justify-between gap-4 border border-slate-200 p-3 transition hover:border-slate-300"><span><span className="block text-sm font-semibold text-slate-800">{label}</span>{description ? <span className="mt-1 block text-sm leading-5 text-slate-500">{description}</span> : null}</span><input id={id} type="checkbox" checked={checked} onChange={(event) => onChange(event.target.checked)} className="mt-1 h-4 w-4 shrink-0 accent-indigo-700 focus:ring-2 focus:ring-indigo-300" /></label>; }
