import { SlidersHorizontal, X } from "lucide-react";

const selectClass = "mt-1 w-full border border-slate-300 bg-white px-3 py-2.5 text-sm text-slate-800 outline-none focus:border-cyan-600 focus:ring-2 focus:ring-cyan-100";
export default function ReportsFilters({ filters, onChange, vehicles, onClear, active }) {
  return <section className="border border-slate-200 bg-white p-4 shadow-sm"><div className="flex items-center gap-2 border-b border-slate-100 pb-3 text-sm font-bold text-slate-800"><SlidersHorizontal size={17} className="text-indigo-600" aria-hidden="true" /> Report filters</div><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6"><label><span className="text-xs font-bold uppercase tracking-[0.1em] text-slate-500">Start date</span><input type="date" value={filters.start} onChange={(e) => onChange("start", e.target.value)} className={selectClass} /></label><label><span className="text-xs font-bold uppercase tracking-[0.1em] text-slate-500">End date</span><input type="date" value={filters.end} onChange={(e) => onChange("end", e.target.value)} className={selectClass} /></label><label><span className="text-xs font-bold uppercase tracking-[0.1em] text-slate-500">Vehicle</span><select value={filters.vehicle} onChange={(e) => onChange("vehicle", e.target.value)} className={selectClass}><option value="all">All vehicles</option>{vehicles.map((vehicle) => <option key={vehicle.id} value={vehicle.id}>{vehicle.registration_number}</option>)}</select></label><label><span className="text-xs font-bold uppercase tracking-[0.1em] text-slate-500">Vehicle type</span><select value={filters.type} onChange={(e) => onChange("type", e.target.value)} className={selectClass}><option value="all">All types</option>{[...new Set(vehicles.map((v) => v.vehicle_type))].map((type) => <option key={type} value={type}>{type}</option>)}</select></label><label><span className="text-xs font-bold uppercase tracking-[0.1em] text-slate-500">Region</span><select value={filters.region} onChange={(e) => onChange("region", e.target.value)} className={selectClass}><option value="all">All regions</option>{[...new Set(vehicles.map((v) => v.region))].map((region) => <option key={region} value={region}>{region}</option>)}</select></label><div className="flex items-end">{active ? <button type="button" onClick={onClear} className="inline-flex w-full items-center justify-center gap-2 border border-slate-300 px-3 py-2.5 text-sm font-semibold text-slate-700 hover:bg-slate-50"><X size={16} aria-hidden="true" /> Clear filters</button> : null}</div></div></section>;
}
