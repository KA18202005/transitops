import { Line, LineChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { Chart } from "@/components/reports/UtilizationChart";
export default function FuelEfficiencyChart({ data }) { return <Chart title="Fuel consumption & efficiency" subtitle="Distance per litre across reporting periods"><ResponsiveContainer width="100%" height="100%"><LineChart data={data} margin={{ left: -18, right: 8 }}><CartesianGrid vertical={false} stroke="#e2e8f0" /><XAxis dataKey="name" axisLine={false} tickLine={false} tick={{ fontSize: 12, fill: "#64748b" }} /><YAxis axisLine={false} tickLine={false} tick={{ fontSize: 11, fill: "#64748b" }} /><Tooltip formatter={(v) => [`${Number(v).toFixed(2)} km/L`, "Efficiency"]} /><Line type="monotone" dataKey="efficiency" stroke="#0e7490" strokeWidth={2.5} dot={{ fill: "#0e7490", r: 3 }} /></LineChart></ResponsiveContainer></Chart>; }
