import { formatCurrency, formatNumber, formatPercent } from "@/components/reports/reportUtils";
export default function VehiclePerformanceTable({ rows }) { return <section className="border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-1 border-b border-slate-200 px-4 py-4 sm:px-5"><h2 className="text-sm font-bold text-slate-900">Vehicle performance ledger</h2><p className="text-sm text-slate-500">Financial and operating return by vehicle for the selected period.</p></div><div className="overflow-x-auto"><table className="min-w-[1180px] w-full divide-y divide-slate-200"><thead className="bg-slate-900"><tr>{["Vehicle", "Registration", "Distance", "Fuel Used", "Fuel Efficiency", "Fuel Cost", "Maintenance Cost", "Revenue", "Operational Cost", "ROI"].map((heading) => <th key={heading} className="px-4 py-3 text-left text-[11px] font-bold uppercase tracking-[0.1em] text-slate-300">{heading}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{rows.map((row) => <tr key={`${row.vehicle_id}-${row.date}`} className="hover:bg-slate-50"><td className="px-4 py-4 text-sm font-semibold text-slate-800">{row.vehicle?.vehicle_name || "Unknown vehicle"}</td><td className="px-4 py-4 text-sm text-slate-600">{row.vehicle?.registration_number || "—"}</td><td className="px-4 py-4 text-sm text-slate-700">{formatNumber(row.distance)} km</td><td className="px-4 py-4 text-sm text-slate-700">{formatNumber(row.fuel_used)} L</td><td className="px-4 py-4 text-sm text-slate-700">{Number.isFinite(row.fuelEfficiency) ? `${row.fuelEfficiency.toFixed(2)} km/L` : "Not available"}</td><td className="px-4 py-4 text-sm text-slate-700">{formatCurrency(row.fuel_cost)}</td><td className="px-4 py-4 text-sm text-slate-700">{formatCurrency(row.maintenance_cost)}</td><td className="px-4 py-4 text-sm font-semibold text-indigo-700">{formatCurrency(row.revenue)}</td><td className="px-4 py-4 text-sm font-semibold text-slate-800">{formatCurrency(row.operationalCost)}</td><td className="px-4 py-4 text-sm font-bold text-cyan-700">{formatPercent(row.roi)}</td></tr>)}</tbody></table></div></section>; }
