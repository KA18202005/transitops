import { CalendarDays, CircleCheck, Clock3, Mail, Phone, ShieldCheck } from "lucide-react";
export default function ProfileSummary({ user }) { const initials = user.full_name.split(/\s+/).map((part) => part[0]).join("").slice(0, 2).toUpperCase(); const fields = [{ label: "Email", value: user.email, icon: Mail }, { label: "Phone", value: user.phone, icon: Phone }, { label: "Role", value: user.role, icon: ShieldCheck }, { label: "Member since", value: new Date(`${user.created_at}T00:00:00`).toLocaleDateString("en-IN", { day: "numeric", month: "long", year: "numeric" }), icon: CalendarDays }]; return <section className="border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 bg-slate-50 p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center"><div className="flex h-20 w-20 items-center justify-center border border-cyan-300 bg-cyan-50 text-2xl font-bold tracking-tight text-cyan-800">{initials}</div><div><h2 className="text-2xl font-semibold text-slate-950">{user.full_name}</h2><div className="mt-2 flex flex-wrap items-center gap-2"><span className="inline-flex items-center gap-1 border border-emerald-200 bg-emerald-50 px-2 py-1 text-xs font-bold text-emerald-700"><CircleCheck size={14} aria-hidden="true" /> {user.is_active ? "Active account" : "Inactive account"}</span><span className="inline-flex items-center gap-1 text-xs text-slate-500"><Clock3 size={14} aria-hidden="true" /> Last active {user.last_active}</span></div></div></div></div><dl className="grid divide-y divide-slate-100 sm:grid-cols-2 sm:divide-x sm:divide-y-0">{fields.map(({ label, value, icon: Icon }) => <div key={label} className="p-4"><dt className="flex items-center gap-2 text-xs font-bold uppercase tracking-[0.1em] text-slate-500"><Icon size={15} aria-hidden="true" /> {label}</dt><dd className="mt-2 break-words text-sm font-semibold text-slate-800">{value}</dd></div>)}</dl></section>; }
